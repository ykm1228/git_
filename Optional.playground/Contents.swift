//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"
// nill - 값이 없음 optional - 값이 있는지 없는지 모름

var firstInt: Int?

firstInt=3

print(firstInt)
print(firstInt!)
// 여기서 firstInt=3을 없에면 12번이 죽어 여기서 12번도 죽이면 nill - 메모리 속 공간에 값이 없다. 공간만 존재한다.
// 복붙 사용하렴 자동완성도 사용하렴.
// 함수(function) - 뭘 넣으면 뭐가 나오는거 코디에서는 넣는 수를 파라미터값=인자 나오는 수를 리턴값
// 함수에는 4가지 모양. 1. 파라미터 리턴 둘다 있는 2. 리턴만 있는 3. 파라미터만 있는 4. 파라미터 리턴 둘다 없는
// swift 에서는 func 함수이름 (파라미터1의 타입 : 파라미터의 이름) -> 리턴 값의 타입{내용 return 리턴 값(정해진 타입의 변수 혹은 상수)

